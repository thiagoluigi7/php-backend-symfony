{% extends 'base.html.twig' %}

{% block title %}Persons{% endblock %}

{% block body %}
    <h1>Persons</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ person.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ person.name }}</td>
            </tr>
            <tr>
                <th>Nis</th>
                <td>{{ person.nis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_persons_index') }}">back to list</a>

    <a href="{{ path('app_persons_edit', {'id': person.id}) }}">edit</a>

    {{ include('persons/_delete_form.html.twig') }}
{% endblock %}
